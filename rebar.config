{sub_dirs, ["apps"]}.
{deps, [
]}.
{port_env, [
	{"CFLAGS", "$CFLAGS $(pkg-config --cflags cairo) -O2 -g"},
	{"LDFLAGS", "$LDFLAGS $(pkg-config --libs cairo)"}
]}.
{port_specs, [
	{"priv/cairerl_nif.so", ["c_src/*.c"]}
]}.
